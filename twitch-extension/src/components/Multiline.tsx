import styled from "styled-components";

interface Props {
  text: string;
}

// const Multiline: React.FunctionComponent<Props> = ({text}) => {
//   const parts = text.split("\n");

//   if (parts.length < 2) {
//     return text;
//   }

//   parts.
// };

export default styled.div`
  white-space: pre-line;
`;

// export default Multiline;
